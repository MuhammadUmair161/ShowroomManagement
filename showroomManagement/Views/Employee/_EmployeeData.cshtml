@model IEnumerable<showroomManagement.Models.Employee>
@*@foreach (var item in Model)
{
    <partial name="_EmployeeList",item></partial>
}*@


<h1>hello</h1>
@*<table class="table table-hover">
    <thead>
        <tr>


            <th>FirstName</th>
            <th>LastName</th>
            <th>Email</th>
            <th>Cnic</th>
            <th>Contact</th>
            <th>Address</th>
            <th>Qualification</th>
            <th>DateOfBirth</th>
            <th>Department</th>

        </tr>
    </thead>
    <tbody>
        <tr>
            @foreach(var item in Model)
            {

                <td>@item.FirstName</td>
                <td>@item.LastName</td>
                <td>@item.Email</td>
                <td>@item.Cnic</td>
                <td>@item.Contact</td>
                <td>@item.Address</td>
                <td>@item.Qualification</td>
                <td>@item.DateOfBirth</td>
                <td>@item.Department</td>
                <td>
                    <a class="btn btn-primary" asp-route-id="@item.Id" asp-action="update">update</a>
                    <a class="btn btn-danger" asp-route-id="@item.Id" asp-action="Delete">Delete</a>
                </td>

            }
        </tr>
    </tbody>
</table>*@

